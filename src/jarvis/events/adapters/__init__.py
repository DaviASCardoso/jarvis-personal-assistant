"""Adapters do Event System: implementações concretas dos ports do Core.

Nada aqui é importado por módulos do Core — a direção da dependência aponta para
dentro (ADR-0001) e é verificada por `tests/test_events_architecture.py`.
"""
